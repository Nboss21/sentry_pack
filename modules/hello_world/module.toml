[module]
id = "dev.hello_world"
name = "Hello World"
description = "Trivial smoke-test module. Exists to give the API and GUI something real to register and invoke before any feature modules are built."
author = "SentryPack Core Team"
version = "0.1.0"
category = "dev"

[[options]]
name = "GREETING"
description = "A greeting string to include in the finding description."
type = "string"
required = false
default = "Hello, SentryPack!"
